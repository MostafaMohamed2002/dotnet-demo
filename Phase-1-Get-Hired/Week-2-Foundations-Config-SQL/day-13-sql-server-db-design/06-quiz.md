# QUIZ
Answer all 5 questions. Paste your answers into the chat when ready.

1. **Conceptual**: You have a `Patients` table and a `Addresses` table. If you store the `CityName` and `CityZipCode` in the `Patients` table, which Normal Form are you violating, and why?
2. **Code-Based**: Write a SQL snippet to create a non-clustered index on the `Email` column of the `Users` table.
3. **Scenario**: You need to find all patients who have *never* booked an appointment. Which type of `JOIN` (INNER, LEFT, RIGHT) would you use, and what condition would you check for in the `WHERE` clause?
4. **Conceptual**: What is the difference between a Clustered Index and a Non-Clustered Index in terms of how the data is physically stored?
5. **Scenario**: You are reviewing a legacy codebase and see a Stored Procedure that contains 500 lines of complex business logic for calculating insurance premiums. Should you move this logic to C# or keep it in SQL? Give one reason for your choice.
