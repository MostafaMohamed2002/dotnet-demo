# QUIZ
Answer all 5 questions. Paste your answers into the chat when ready.

1. **Conceptual**: In what order does .NET load configuration? If a key exists in both `appsettings.json` and an Environment Variable, which one is used?
2. **Code-Based**: You have a class `ClinicSettings` and a JSON section called `"Clinic"`. Write the single line of code in `Program.cs` that binds this section to the class.
3. **Scenario**: You are working in a team. You need to share the `appsettings.json` structure with your colleagues, but you cannot share your local database password. What is the correct .NET tool/feature to use for this password?
4. **Conceptual**: What is the purpose of the `.Value` property when using `IOptions<T>`?
5. **Scenario**: Your app works perfectly on your machine but fails in the Production Docker container because it can't find the `ConnectionStrings:DefaultConnection`. You've checked that the JSON file is there. What is the most likely reason, and how should the connection string be provided in a containerized environment?
